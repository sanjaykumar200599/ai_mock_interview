import { Interview } from "@/types"

interface InterviewPinProps{
    interview: Interview;
}

export const InterviewPin = ({interview}: InterviewPinProps) => {
  return (
    <div>InterviewPinProps</div>
  )
}
